---
description: Git commit message conventions for all Realethia repos
alwaysApply: true
---

# Git commit messages

Org-wide rule for all Realethia repositories. Lives in `realethia-start` only — applied via the multi-root workspace, not copied per repo.

All Realethia repositories use **Conventional Commits** with this subject format:

```
type: Short description starting with a capital letter.
```

## Types

| Type | Use for |
|------|---------|
| `feat` | New feature or user-facing capability |
| `fix` | Bug fix |
| `docs` | Documentation only |
| `chore` | Tooling, deps, config, repo maintenance |
| `refactor` | Code change that is not a feat or fix |
| `test` | Tests only |
| `ci` | CI/CD pipeline changes |
| `perf` | Performance improvement |

## Rules

1. **Subject format:** `type: Description` — lowercase type, colon + space, description starts with a capital letter.
2. **Example:** `feat: Separate setup from start for local bootstrap.`
3. **Keep subjects concise** (one line). Add a body only when the user asks or the change needs brief context.
4. **Commit only when asked** — do not create commits unless the user explicitly requests it.
5. **Match scope** — one logical change per commit; message describes the *why*, not a file list.

## Authorship (strict)

Commits must be **solely attributed to the human user**. Agents must never add themselves as co-authors.

**Never add** any of these trailers:

- `Co-authored-by:`
- `Co-Authored-By:`
- `Signed-off-by:` (on behalf of an agent)
- `Assisted-by:`, `Made-with:`, or similar tool/agent attribution lines

**Never mention** Cursor, Copilot, Claude, or other AI tools in commit messages or trailers.

If a git hook or tool injects a co-author trailer (e.g. `Co-authored-by: Cursor <cursoragent@cursor.com>`), **remove it before the commit lands**. Use `git commit-tree` or amend with a clean message if `git commit` cannot skip the hook.

The final commit message must contain **only** the intended subject (and optional body) — nothing else.

## Examples

```
feat: Add RSS test server Makefile target.
fix: Prevent duplicate feed registry writes on CDC replay.
docs: Document local bootstrap workflow in README.
chore: Sync agent rules to sibling repos on setup.
```
